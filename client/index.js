import React from 'react';
import ReactDOM from 'react-dom';

const title = 'React with Webpack and Babel';

ReactDOM.render(
  <div>React with Webpack and Babel</div>,
  document.getElementById('app')
);